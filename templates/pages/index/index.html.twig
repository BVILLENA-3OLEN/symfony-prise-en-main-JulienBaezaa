{% set page_title = 'app.index.title'| trans %}

{% extends "base.html.twig" %}
    
{# Commentaire twig #}

{# Ce fichier est le template de la page d'accueil #}
{# Il étend le template de base et définit le titre de la page #}


{% block stylesheets %}
    <link href = "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src = "https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
{% endblock %}



{%block body%}
    <h2>Bienvenue sur 3OLEN</h2>
    {%if nom is not null%}
        <p> Mon nom est : {{nom}} </p>
        <a href="{{ path('app_hello_name_get', {'name': 'nom'}) }}">Hello index</a>

        <article>
            <header>
                <h1>Articles</h1>
            </header>
            <nav>
                {% if can_create %}
                    <a href="{{ path('app_post_create_get') }}" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Formulaire Création</a>
                {% endif %}
            </nav>
            <main>
                <ul>
                    {% for published_post in published_posts%}
                        <li>
                            <span class="badge bg-info text-white">
                                {{ published_post.publishedAt | date('d/m/Y à H:i') }}
                            </span>
                            {{ published_post.title }}
                            <a href="{{ path('app_post_show_get', {'id': published_post.id}) }}"class="btn btn-info">
                                👀
                            </a>
                        </li>                             
                    {% endfor%}
                </ul>
            </main>
        
        </article>
    
            
    {% endif%}


{% endblock %}




